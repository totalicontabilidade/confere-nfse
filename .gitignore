# Nada aqui vai para o repositório: são dados de cliente e credenciais.
# Atenção: no .gitignore, comentário só vale em linha própria.

# PDFs fiscais enviados e o texto lido deles
cache/

# conferência em andamento (notas de clientes)
sessao.json

# certificado digital escolhido e ponto da consulta ao portal
portal.json

# modelos ensinados: podem trazer CNPJ de fornecedores
modelos.json

# certificados, em qualquer formato
*.pfx
*.p12
*.key
*.pem

# arquivos reais de trabalho que por acaso caiam na pasta
*-teste.pdf
testes/fev*.pdf
testes/01-*.pdf
testes/02-*.pdf

# lixo do sistema
__pycache__/
*.pyc
*.bak
*.tmp
.DS_Store
Thumbs.db
desktop.ini

# configuracao do Firebase desta instalacao
firebase-config.js

# acesso pela rede do escritorio: o codigo (com hash) e o segredo do cookie
acesso.json

# uma conferencia em andamento por computador
sessoes/

# log do servidor e aviso de parada
servidor.log
parar.sinal

# plano de contas e memoria de classificacao de cada empresa
planos/
